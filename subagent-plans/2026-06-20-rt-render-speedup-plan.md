{
  "summary": "Audit the self-written CUDA RT renderer for speedups (Isaac-Lab-referenced); produce a verified, prioritized plan incl. an owner-gated OptiX decision",
  "agentCount": 25,
  "logs": [
    "[stall] agent \"res:isaac-lab\" stalled (no progress) after 265s — retrying (1/5)",
    "[stall] agent \"res:cuda-fast-rt\" stalled (no progress) after 295s — retrying (1/5)",
    "mapped 4/4 dimensions, researched 4/4 topics",
    "generated 15 proposals",
    "verified 15 proposals, 13 judged real"
  ],
  "result": {
    "plan": "All load-bearing claims confirmed:\n- **Download-mask (P11 re-scope):** `FramebufferToReport` reads ONLY `frame.prim` and `frame.color` — depth/normal/albedo/uv are downloaded (6 `CopyToHost` calls, lines 1069-1074) but never consumed by the mp4 path. Confirmed real, zero-risk D2H cut.\n- **Per-frame churn:** 6 fresh `OwnedBuffer` allocs/frees per frame + `BuildFrameTlas` rebuild (lines 1032-1075). Confirmed.\n- **s==0 AOV gate:** geometry/material AOVs written only at center sample (line 675), color is the 16-sample average — confirmed denoiser setup.\n- **`--fmad=false`** on RT libs + header's explicit \"G1-B dropped/deferred\" for wide-BVH/sorting/persistent-threads + total-order tie-break on prim id — all confirmed.\n\nI have everything needed. Writing the roadmap from the verified data.\n\n# RT Renderer Speedup Plan\n\n## Exec summary\n\nCurrent cost: **~11.6 s/frame @1080p**, **~2.9 s @960×540** (linear 4× in pixels → the kernel is per-pixel ray-bound, *not* fixed-overhead-bound), so a **350-frame 1080p clip ≈ 68 min**. The frame is a one-thread-per-pixel megakernel (`src/phi/backend_cuda/rt/two_level_render.cu` `RenderBeautyKernel` ~L614) doing **up to 176 nested TLAS→BLAS closest-hit traversals/pixel** = `S·(1 + K + M·(1+gi))` with `S=16, K=4 shadow, M=3 AO, gi=1` (ShadeBeauty L516–608). Every slab/triangle/sphere test runs **FP64 with `--fmad=false`** (CMakeLists.txt:1068, 1031–1053) — kept that way ONLY so the *gated* `RenderFrame` golden stays byte-exact against the host oracle. `RenderBeauty` is already a **separate, stochastic, non-golden** kernel, which is what makes almost every win below pillar-safe.\n\n**The realistic stacked Tier-1 target (use these, not the optimistic proposer numbers):**\n\n| Lever | Realistic factor | What it multiplies |\n|---|---|---|\n| P1 FP32 + FMA on a beauty-only TU | **~2×** (1.6–3×) | per-ray cost |\n| P2 A-trous denoiser → SPP 16→3–5 | **~3–4×** (bankable A-trous tier; ~8× only with the unbuilt P6+SVGF temporal chain) | ray *count* |\n| P5 AO-radius tMax prune + shadow any-hit | **~1.2–1.4×** | visibility/AO traversal |\n| P8 `__launch_bounds__` occupancy | **~1.05–1.3×** | kernel-only |\n| P11 download-mask (drop 4 unused AOVs) | **~1.01–1.05×** | host D2H tail |\n\nThese are **sub-multiplicative** where they contend the same memory/traversal bottleneck (P1×P5×P9 overlap; P2 is the cleanly-orthogonal one since it cuts ray *count*). Honest combined math: `2 × 3.5 × 1.3 × 1.15 × 1.03 ≈ 10.8×` raw, de-rated to **~6–8× realized** after warp-divergence and shared-bottleneck haircuts → **~1.5–2 s/frame @1080p**, turning the **68-min clip into ~9–12 min**. Push to the **~4–6 min** range needs the structural Tier-2 work (wide-BVH P9, wavefront P10) *and/or* the temporal denoise chain (P6→P7→SVGF), each of which is high-effort and carries golden-regeneration cost. **Iteration latency** is solved separately by the preview tier (P12, ~6–12× on look-dev only) and **export wall-time** by multi-GPU (P14, ~Nx, owner-gated on hardware).\n\n---\n\n## Tier 1 — CUDA-only QUICK WINS (ROI-ordered)\n\n### T1.1 — Download only the consumed AOVs (P11, re-scoped sub-lever)\n- **What:** Gate the 6 D2H copies (two_level_render.cu:1069–1074) by an output mask; download only `color` + `prim`. **Verified at source:** `rt_framebuffer_to_report.cpp:49–58` reads ONLY `frame.prim` and `frame.color`; depth/normal/albedo/uv are copied back every frame and never consumed by the mp4 path.\n- **Why:** Removes ~75 MB (~69%) of the ~108 MB/frame D2H at zero kernel/stream/determinism risk. The cleanest isolated win in the set.\n- **Realistic est:** ~1.01–1.05× (PCIe-bound ~6 ms on an 11.6 s frame today; grows as the trace shrinks). **Gate:** when P2's denoiser lands, the mask must re-enable normal/albedo/depth/prim.\n- **Effort:** low (an output-mask flag + conditional copies). **Risk:** very low.\n- **Pillars:** D1/DIFF/DETERMINISM all preserved (identical kernel/bytes).\n\n### T1.2 — `__launch_bounds__` on `RenderBeautyKernel` (P8, sub-claim 1 only)\n- **What:** Add `__launch_bounds__` to `RenderBeautyKernel` (two_level_render.cu:614). Treat `maxrregcount` as a **measure-first** knob — it is per-TU and would also hit `RenderFrameKernel`, risking local-memory spills.\n- **Why:** The inlined two-level traversal + FP64 temporaries inflate registers (zero existing launch_bounds/maxrregcount anywhere in `src/`); capping per-kernel registers can lift occupancy on this fat kernel. Per-kernel attribute leaves the golden `RenderFrameKernel` untouched.\n- **Realistic est:** ~1.05–1.3× kernel-only (NOT the proposer's 1.3–2×). **Caveat:** if the kernel is FP64-*throughput*-bound, occupancy hides latency not throughput — so land this **after/with P1**, which removes the FP64 saturation.\n- **Effort:** low (hours). **Risk:** low; only failure is a perf regression if mis-tuned (byte-exact-safe — register counts don't change IEEE results with FMA already off).\n- **Pillars:** D1/DIFF/DETERMINISM preserved. **Dropped from P8:** the const-ref `DevBlas` sub-claim (the copy is once-per-instance, scalarized to registers — ~nil) and the drop-`ReconstructHit` sub-claim (it edits the shared ClosestHit/ReconstructHit that the `RenderFrame` Oracle-1/Oracle-2 normal memcmp gates depend on → NOT byte-exact-safe, needs a lockstep oracle change; defer).\n\n### T1.3 — FP64→FP32 + FMA on a beauty-only traversal/leaf/raygen TU (P1)\n- **What:** Compile a **second copy** of the traversal + leaf-intersection + raygen math (templatize `RtSlabInterval`/`RayTriangleIntersect`/`RaySphereIntersect`/`RtNormalize`/`GenerateRay` on `<typename Real>`) into a **new beauty-only `.cu` built `--fmad=true`**, instantiated `float`. The byte-exact `RenderFrame` TU keeps the `double` instantiation and `--fmad=false` untouched.\n- **Why:** The hottest instructions (~3e8 traversals/frame) run FP64 with FMA forbidden — on consumer sm_86 that is 1/32–1/64 FP32 throughput. This is the single largest *per-ray* multiplier and is orthogonal to the SPP cut (P2).\n- **Realistic est:** **~2× (1.6–3×), NOT 8–20×.** The 8–20× is the instruction-ratio mis-applied to the whole kernel (Amdahl): this megakernel is latency/memory-bound on incoherent dependent loads (`nodes[child].aabb` in bvh_traverse_impl.cuh; per-instance `DevBlas` chase) and warp divergence on secondary rays, which FP64 ops largely overlap. Real undersold bonus: freeing register pairs can lift occupancy toward the upper end. (Note: the per-instance `QuatInvRotate`/`TransformRayToLocal` is *already* FP32 and gains nothing.)\n- **Effort:** medium-high — a templated `<Real>` alone is **insufficient**; `RenderFrame` and `RenderBeauty` share the same `.cu` and the math is `inline` in shared `.cuh`, so you MUST physically split the beauty kernel into its own `--fmad=true` TU.\n- **Risk:** medium — the shared `.cuh` (intersect_primitives, ray_box, instance_transform) and `camera.hpp` are `#included` by the host oracle; a wrong template default / accidental `float` instantiation in those paths breaks the byte-exact `RenderFrame` golden (DETERMINISM). FP32 slab/Möller-Trumbore is slightly looser at grazing rays → possible silhouette speckle (non-gated, acceptable).\n- **Pillars:** D1 preserved (self-written). DIFF preserved (keep an `fp64` instantiation selectable if beauty AOVs ever feed diff-sim). DETERMINISM: golden `RenderFrame` is a different kernel → byte-identical; beauty bytes change vs old fp64 beauty (acceptable, non-gated).\n\n### T1.4 — A-trous denoiser → drop SPP 16→3–5 (P2, A-trous tier only)\n- **What:** Self-written **edge-avoiding A-trous wavelet** (Dammertz 2010): albedo-demodulate `color`, run 5 separable B3-spline passes (step 1,2,4,8,16) with edge-stopping weights on depth + world-normal + `prim`-id + luminance, remodulate. Then lower `samples` to ~3–5.\n- **Why:** The `S=16` loop (two_level_render.cu:641–679) is a true **~16× linear multiplier** on the whole 11-ray shade with no amortization. **Verified setup:** depth/normal[WORLD]/albedo/uv/prim are written ONLY at `s==0` (center, two_level_render.cu:675) so they are **noise-free at any SPP** while only `out_color` is the 16-sample average — the textbook denoiser input. This is the single biggest *ray-count* lever and is the cleanly-orthogonal multiplier on P1/P5.\n- **Realistic est:** **~3–4× bankable** (A-trous at ~3–5 SPP, code-defensible today). The proposer's 6–12× folds in the **unbuilt** temporal chain (P6 motion vectors + SVGF) and content assumptions; reserve ≥8× as *conditional*.\n- **Effort:** high (~1–2 days for the A-trous first cut; weeks for SVGF). **First sub-task is paying down RT-F2:** make the beauty AOVs **device-resident** — today `RenderBeauty` allocs local `OwnedBuffer`s, runs one kernel, `CopyToHost`s, and frees on return (L1032–1075), so there is no resident AOV for a GPU denoiser kernel to read. It is NOT a free \"pure image-space post-pass.\"\n- **Risk:** medium — A-trous over-blurs per-foot contact shadows and GGX speculars if sigmas are loose; thin dog-leg silhouettes can mis-tag at the 1-px center sample. **The SPP reduction for FINAL exports must be eyeballed on a real clip**, not assumed from the multiplier.\n- **Pillars:** D1 preserved (pure CUDA, unlike OptiX/OIDN). DIFF preserved (a-trous/EMA/variance are differentiable bilateral ops). DETERMINISM: single-frame fixed-order separable A-trous can itself stay byte-exact; ship as a NEW non-gated beauty mode + a deterministic no-denoise toggle. The gated `RenderFrame` is a separate kernel, untouched.\n\n### T1.5 — AO-radius tMax prune + shadow/GI any-hit early-out (P5, re-scoped)\n- **What:** Two *distinct* changes (do NOT call both \"any-hit\"):\n  - **(A) `TraverseRayAnyHit`** functor that returns on the FIRST primitive in `[t_min, max_dist)` — apply ONLY to pure-visibility rays: hard-shadow (L404), soft-shadow (L544), GI-shadow (L594).\n  - **(B) byte-exact `ao_radius` tMax prune** in `want()` (skip child when `et >= max_dist`) for the closest-hit AO ray (L573) — **keep it closest-hit** so the `gi_bounces=1` bounce stays byte-exact.\n- **Why:** ClosestHit is used for ALL ray types (best_t seeded to +inf, L262); ~7 of ~11 rays/sample are visibility-only. (B) is the largest, determinism-safe win: `ao_radius=0.6` lets the TLAS prune nearly all instances instead of finding a nearest hit it then discards.\n- **Realistic est:** **~1.2–1.4× whole-frame**, NOT ~2×. The \"1.3–2× on the visibility portion\" holds only for occlusion-heavy interiors.\n- **Effort:** medium. **Risk:** low. **Two corrections baked in:** (1) the AO ray's nearest hit is *consumed* for the GI bounce when `gi_bounces=1`, so converting it to any-hit breaks byte-exactness — it gets only the tMax prune. (2) The sun is **directional** (`light_dist=+inf`), so \"thread real tMax into want() for shadows\" is a no-op there — the shadow win is purely early-return, and is throttled by SIMT warp divergence (a *lit* ray still traverses the whole frontier to prove non-occlusion).\n- **Pillars:** D1/DIFF/DETERMINISM preserved — boolean visibility is order-invariant (A is even golden-safe); keep on beauty path to be safe. Mind the `eps`/`[t_min,dist)` bounds and `>=` consistency with the existing `bt >= ao_radius` test (L574) to avoid a 1-ULP flip.\n\n### T1.6 — Firefly soft-clamp + zero-weight GI-shadow cull (P3 re-classified + P4 cull-half)\n- **What:** A smooth (differentiable) max-luminance **soft-clamp** on the GGX specular (L556) and 1/π GI term (L597–600); plus cull the **secondary GI shadow ray (L594)** when bounce weight ≈ 0.\n- **Why / honest status:** **P3 is NOT a speedup (1.0×) and is NOT currently an enabler** — exhaustive grep finds zero denoiser/temporal code, and the ACES tonemap (`rt_framebuffer_to_report.cpp:22–37`) already clamps displayed output. Keep it as a **low-cost robustness term that becomes relevant only when T1.4's denoiser + SPP-reduction actually land** (a firefly at low SPP smears into blotches). The P4 cull half gives a real but tiny **~1.03–1.08×** by skipping a slice of the conditionally-fired GI shadow rays.\n- **Realistic est:** **~1.05× (the cull)**; the clamp is 0× standalone, an enabler for T1.4 only.\n- **Effort:** low. **Risk:** low. **Corrections:** do NOT credit P3 with any fraction of P2's speedup; do NOT cull the **AO ray at L573** (its hit is needed for sky-dome ambient regardless of albedo — it is never wasted); the NoL≤0 shadow cull the proposer lists **already exists at L542** (zero new gain). The blue-noise/Sobol half of P4 buys ~0× until a denoiser exists.\n- **Pillars:** D1 preserved. DIFF only if the clamp is a **smooth** soft-clamp (a hard `min()` creates a zero-gradient plateau) and the cull is smoothed/kept off any gradient path. DETERMINISM: beauty-only; set the ceiling above the ACES shoulder so legitimate highlights never dim; verify no golden reads `RenderBeauty`.\n\n---\n\n## Tier 2 — CUDA STRUCTURAL (BVH, wavefront, pipeline)\n\n### T2.1 — Binned-SAH **binary** BLAS builder (P9, first half only)\n- **What:** A **parallel self-written binned-SAH binary builder for the RT BLAS only** (NOT the shared physics `BuildLbvhForQuery`), keeping the existing 36 B node layout and the **verbatim stackless traversal unchanged**, with exact FP32 bounds and deterministic build order. Drop the unused pair-emit while there.\n- **Why:** The BLAS is built ONCE and reused across all 10–330 instances and all ~350 frames (rigid, never refit), so build-quality is **free per frame**. Morton median-split → looser AABBs → more node/leaf tests/ray.\n- **Realistic est:** **~1.15–1.4×** (NOT the generic 1.3–2×) — a single roughly-uniform Go2 mesh is Morton's **best** case; SAH wins most on heterogeneous geometry.\n- **Effort:** high (self-written binned-SAH builder). **Risk:** medium. **Key determinism finding:** because the closest-hit tie-break is a **total order on packed prim id** (confirmed two_level_render.hpp:23–24) and leaf AABBs are identical, an exact-FP32 SAH rebuild is **plausibly byte-exact** for existing goldens — *verify, do not assume*; do not loosen tolerance (CLAUDE.md honesty line).\n- **Pillars:** D1 preserved (self-written builder). DIFF preserved (no quantization). DETERMINISM: plausibly byte-exact; if it perturbs, regenerate goldens honestly.\n\n### T2.2 — Wide BVH8 / CWBVH (P9, second half — DEFER, owner-visible)\n- **What:** Collapse the SAH binary tree into a compressed 8-wide BVH with 8-bit child-bound quantization (Ylitie 2017), with a new wide/short-stack traversal.\n- **Why:** 8-wide nodes amortize memory latency over 8 box tests on exactly the incoherent AO/GI/shadow rays that dominate here.\n- **Realistic est:** **~1.3–1.8× standalone** on incoherent rays — **NOT cleanly stackable** with SAH (wide nodes change the visit accounting SAH reduces) and NOT multiplicative with P1/P5/P10 (shared traversal-step cost). The proposer's \"~2.5–4× stacked\" double-counts.\n- **Effort:** high. **Risk:** high — (1) **conflicts with the \"one verbatim stackless traversal at both levels\" pillar** (two_level_render.cu:5–6) — a wide BVH needs stack-based traversal; (2) a per-level wide stack on ~3e8 nested secondary rays **worsens the register/local-mem spill** the renderer already risks; (3) 8-bit quantization **WILL break goldens** (quantized bounds prune the FP32 winner / perturb entry-t) → regenerate honestly; (4) **reverses the header's stated G1-B deferral** (two_level_render.hpp:27) → owner-visible scope change.\n- **Pillars:** D1 preserved (self-written, quantization is internal not a blob). DIFF partial (quantized bounds add small non-diff steps; keep hit/shade math FP32). DETERMINISM: not byte-exact → parallel path + honest golden regen. **Recommendation: ship T2.1 first; gate T2.2 on T1 leaving the target unmet.**\n\n### T2.3 — Wavefront / persistent-threads with secondary-ray compaction (P10)\n- **What:** Restructure the beauty megakernel into a persistent-threads / wavefront pipeline with SoA path-state + compacted secondary-ray queues (optionally Morton-sorted).\n- **Why:** Single megakernel, 16×16 one-thread-per-pixel, no queues; sky lanes (1 ray) share warps with covered lanes (~11 rays) and AO-hit/escape branches diverge.\n- **Realistic est:** **~1.1–1.4× on the beauty trace portion** (NOT 1.5–2.5×); **net could be ≤1.0× (regression)** on this kernel. **Premise corrections that shrink the win:** this kernel is **already stackless** (Hapala parent-pointer, bvh_traverse_impl.cuh) so the classic wavefront occupancy win (eliminating the traversal stack) is mostly *already pulled*; it is **iterative not recursive** (no recursion to flatten); the divergence is per-ray traversal *length* + early-`continue`, which wavefront only partly helps; and K/M/S are uniform constants so every lane issues the same *number* of ClosestHit calls. pbrt-v4/Mitsuba3 data show wavefront can **LOSE** to a megakernel at low material divergence — which is exactly this single-Lambert+GGX kernel.\n- **Effort:** high (architectural rewrite). **Risk:** high.\n- **Pillars:** D1 preserved (self-written scheduling). DIFF partial (multi-kernel SoA harder to keep differentiable). DETERMINISM at risk — full compacted/sorted queues make per-pixel sample order data-dependent → **beauty path only**; the D1 `RenderFrame` path keeps the megakernel (or persistent-threads with atomics on the **work counter only**, ClosestHit nest verbatim, fixed per-pixel reduction). **Strictly last; contingent on P1 landing first** (FP64+`--fmad=false` is a co-dominant cost wavefront cannot touch), and on a real (later) divergence profile confirming a secondary-ray utilization deficit.\n\n### T2.4 — Pipeline overlap: streams + pinned staging + persistent buffers (P11, full version)\n- **What:** Persistent device AOV/instance/TLAS buffers (sizes constant for fixed res + instance count), pinned-host AOV staging (the phi pinned `BufferType` already exists), async D2H on a copy stream, double-buffered to overlap download/encode N with trace N+1.\n- **Why:** The host tail is 100% GPU-idle: `BuildFrameTlas` re-uploads + rebuilds the TLAS and 6 AOV buffers are freshly `cudaMalloc`/freed **every frame** (~124 MB churn @1080p, confirmed L1032–1075), 6 synchronous pageable D2H copies, single default stream.\n- **Realistic est:** **~1.01–1.05×** today (low-single-digit % of an 11.6 s trace); matters more once the trace is fast. **Contingent, not a frames-to-minutes lever.**\n- **Effort:** medium (a real cross-layer `RtBackendI`/`TraceBeautyToHost` API change + 2-deep pipeline + pinned double-buffers — NOT \"wiring\"). **Risk:** low-medium. **Corrections:** the TLAS must be **REBUILT, never refit** to stay byte-exact (`BuildLbvhImpl` uses `thrust::stable_sort_by_key` for D1 order; a refit changes pixels when instance Morton order shifts); TLAS-scratch persistence requires refactoring `BuildLbvhImpl`, which is **shared with the gated collision broadphase** — size effort accordingly; pinned dest is **mandatory** for async correctness (pageable silently degrades to sync); the per-frame whole-mesh host re-copy (`MeshToBlas`) is host-only CPU work — hoist it out of the loop separately.\n- **Pillars:** D1/DIFF/DETERMINISM preserved (identical kernels/bytes, scheduling only) — but it touches the stream discipline the codebase flags as the \"D1 anchor\", so verify byte-exactness.\n\n### T2.5 — Direct-to-encoder pipe (P13) — convenience only, not a perf lever\n- **What:** Pipe tonemapped RGB to `ffmpeg` stdin instead of writing PPMs.\n- **Realistic est:** **~1.0005× (1080p)** — PPM write is ~6 ms on an 11.6 s frame. **Implement ONLY on top of T2.4** with a worker-thread/bounded-queue so a slow encoder cannot back-pressure the render thread; keep disk-PPM for golden paths and route golden compares off the pre-encode bytes. The cited \"2 GB read-back pass\" belongs to the **separate lavapipe `go2_walk_video` demo**, not the RT path (which has no ffmpeg call). All overlap credit belongs to T2.4.\n- **Pillars:** all preserved (pre-encode bytes identical; ffmpeg is an existing runtime dep, not a linked SDK). **Risk:** low; not a megakernel speedup.\n\n### Iteration & export levers (orthogonal to per-frame trace)\n\n**T2.6 — Fast PREVIEW preset (P12):** expose the budget knobs (replace the hardcoded `shadow_rays`/`gi_bounces`/`ao_samples` in `BeautyFromOptions`, go2_terrain_demo.cpp:671–690) behind a `--preview` flag; `--width/--height/--samples` are **already plumbed**. Half-res alone = **~4× (measured)**; full preset (low SPP, `ao_samples=0`, `gi_bounces=0`) = **~6–12× typical** on the trace, NOT the 88× raw ray ratio (background pixels already cost 1 ray; the per-frame TLAS rebuild is a fixed floor). **0× on final-export wall time.** Label it a **flat/hard-shadow preview** — with `ao_samples=0`/`gi_bounces=0` the AO + GI *components* vanish (a denoiser cannot reconstruct un-sampled energy); for faithful look-dev prefer `gi_bounces=0` + `ao_samples=1–2` + `shadow_rays=1` at half-res. Effort low, risk low, all pillars preserved (pure config).\n\n**T2.7 — Multi-GPU frame-parallel export (P14, `needs_owner_signoff=true`):** shard the 350 independent deterministic frames across N GPUs/processes (`CUDA_VISIBLE_DEVICES` per process — zero renderer change). **~0.85N–0.95N** on total clip wall-time only (e.g. ~3.7–3.9× at N=4), **1× if only one GPU is available**, and **risks OOM** with N concurrent TLAS+framebuffer allocations on one box. Must add a global frame-offset or per-shard out-dir (the `frame_%06u.ppm` counter resets to 0 per process → collisions). **Owner-gated ONLY because it may need provisioning extra GPU hardware** (and the memory notes a single-GPU training constraint — confirm render may use more). Per-frame cost and previews unchanged. All pillars preserved.\n\n---\n\n## Tier 3 — OPTIX DECISION (owner go/no-go)\n\n**Honest cost.** OptiX is a header-only SDK whose symbols load from the **NVIDIA display driver** at runtime — no static blob, but a **hard driver dependency** (OptiX 9.0 R570+/9.1 R590+; RT-core HW Turing+; Maxwell fallback gets **no** RT win). Adoption = write/compile ray programs to PTX/OptiX-IR, build modules→program-groups→pipeline + Shader Binding Table + `optixAccelBuild` GAS+IAS. The **IAS-over-one-shared-GAS** pattern maps *exactly* onto \"300 instances of one Go2 mesh + terrain\" and reuses the BLAS-once/TLAS-refit design.\n\n**Expected RT-core speedup (this workload):** **~2–4×** for RT-core traversal (Cycles RTX 2080 Ti 2.2×; OptiX 7.0 ~2×). The 10×–400× figures are incoherent *non-graphics* workloads and **do not apply**. 1080p 11.6 s → ~3–6 s; 68-min clip → ~20–35 min. The **standalone OptiX AI denoiser** (samples 16→2–4 via the albedo+normal AOVs the kernel already emits) is **~4–8× ceiling** and is the bigger practical lever with a far smaller seam.\n\n**What it breaks:**\n- **D1 (self-written):** **VIOLATED** — AS build + RT-core traversal + AI-denoiser are closed driver-resident black boxes. This is the blocker.\n- **DIFF:** compatible *in principle* (Mitsuba3/Dr.Jit use OptiX as the intersection backend) but **single-precision only and no gradients THROUGH the opaque AS**; the AI denoiser is a non-differentiable CNN.\n- **DETERMINISM:** **PARTIAL** — OptiX guarantees same-input→same-output only *within* a GPU architecture; AS layout \"may vary from one GPU architecture to another\" → goldens become **per-GPU-model**, not the current bit-exact-on-any-CUDA-device path. *Scope correction:* this concern attaches **only to the OptiX-traversal variant on the golden `RenderFrame` path** — the **denoiser-on-beauty** variant leaves the byte-exact sensor goldens untouched, so it is even safer on determinism than a blanket \"PARTIAL,\" while still being a D1/DIFF violation. (Also: there is **no motion AOV / temporal state** yet, so only single-frame denoising is available out of the box.)\n\n**Strongest CUDA-only alternative that gets most of the win:** the Tier-1+Tier-2 stack **P1 + P5 + (T2.1 SAH) + T2.3** is the pillar-safe analog of RT-core traversal (~2–4×), and **T1.4 self-written A-trous/SVGF over the same AOVs** is the pillar-safe analog of the OptiX denoiser at comparable speedup. OptiX's **incremental ceiling over a de-divergenced FP32 wide-BVH CUDA path is only ~1.5–2×** — at the cost of the D1 pillar, a driver dependency, and per-GPU goldens.\n\n**RECOMMENDATION: NO-GO for now.** Pursue the CUDA-only stack first — it preserves every pillar and recovers most of the win. Bring OptiX back to the owner as an explicit decision **only if** the CUDA stack lands and still misses the minutes target. If *any* OptiX is then approved, adopt the **standalone single-frame beauty-path denoiser first** (largest win, smallest seam, determinism-safe on the sensor goldens, leaves the tracer self-written) before ever touching RT-core traversal. `needs_owner_signoff=true` for all OptiX options.\n\n---\n\n## First implementation slice (smallest change, biggest measured payoff)\n\nLand in this order — each is independently shippable and verifiable, lowest-risk first so we bank wins before the risky refactors:\n\n1. **T1.1 download-mask** (hours, zero-risk, isolated) — verified that only `color`+`prim` are consumed.\n2. **T1.2 `__launch_bounds__`** on `RenderBeautyKernel` only (hours; per-kernel so goldens untouched).\n3. **T1.5(B) AO-radius tMax prune** in `want()` (byte-exact, the determinism-safe half of P5) + **T1.5(A) shadow any-hit** functor.\n4. **T1.3 FP32 beauty TU** — the **highest single payoff** (~2×); the surgical split of the beauty kernel into its own `--fmad=true` TU is the load-bearing risk, so do it after the cheap wins are banked and with the oracle-shared `.cuh` carefully templatized.\n\nThat sequence is expected to deliver **~2.5–3.5×** (≈ **3–4.5 s/frame**, ≈ **18–26 min/clip**) before any denoiser or structural rewrite. T1.4 (A-trous + SPP cut) is the next slice and the largest remaining lever, but it gates on making AOVs device-resident (RT-F2) and on an eyeball quality check.\n\n## Profiling plan (run ONCE the GPU frees — measure, never guess)\n\nReason statically until then; the moment the in-flight 1080p render finishes:\n\n1. **Baseline ground-truth:** Nsight Systems timeline on one 1080p frame to split **trace kernel time vs host tail** (validates the static \"trace dominates, tail ~1–4%\" claim that ranks P11/P13 low). Capture `RenderBeautyKernel` duration, the 6 D2H copies, and `BuildFrameTlas`.\n2. **Kernel characterization (Nsight Compute on `RenderBeautyKernel`):** confirm the **memory/latency-bound** hypothesis (sm_efficiency, achieved occupancy, `dram__throughput`, `l2_tex_read_hit_rate`, warp stall reasons). **This decides P1's true multiplier** (if FP64-pipe-utilization is high → P1 lands toward 3×; if memory-stall-dominated → toward 1.6× and P9/P10 matter more) and **whether P10 wavefront is worth it** (need a real secondary-ray warp-utilization deficit, since the kernel is already stackless).\n3. **Register/occupancy:** `--ptxas-options=-v` register count + the occupancy-vs-`maxrregcount` curve to set T1.2 conservatively (avoid local-mem spills) and to confirm P1's register-freeing occupancy bonus.\n4. **A/B harness:** render a fixed 8–16-frame slice of the real Go2-terrain clip (fixed seed) before/after **each** lever; record s/frame **and** SSIM/PSNR vs the 16-SPP reference so the **SPP-reduction quality is eyeballed on real content** (per P2's honesty caveat) — never lower export SPP on the multiplier alone.\n5. **Golden guard after every change:** run the gated `RenderFrame`/sensor goldens to prove byte-exactness held (especially after T1.3's TU split, T2.1's SAH rebuild, and any T2.4 stream/refit change); if a golden legitimately moves, regenerate honestly — never loosen tolerance (CLAUDE.md).\n\n**Files for the slice:** kernel `src/phi/backend_cuda/rt/two_level_render.cu` (RenderBeautyKernel ~614, ShadeBeauty 516–608, ClosestHit 254–271, AOV download 1069–1074, AOV alloc 1032–1039); traversal `src/phi/backend_cuda/rt/bvh_traverse_impl.cuh` (add any-hit functor + tMax into `want()`); shared math to templatize `src/phi/backend_cuda/rt/{ray_box.cuh,intersect_primitives.cuh,instance_transform.cuh}` + `src/rt/camera.hpp`; build flags `src/CMakeLists.txt:1068` (new beauty TU = `--fmad=true`); consumers `src/render/rt_framebuffer_to_report.cpp:49–58`; budget knobs `examples/demo/go2_terrain_demo.cpp:671–690`.",
    "num_proposals": 15,
    "num_verified_real": 13,
    "maps_done": 4,
    "research_done": 4
  }
}