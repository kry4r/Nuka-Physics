--- L2177 ---
The C ABI applies both knobs at `src/c_abi/world.cpp:485-486`; only the Python binding for `create_from_scene` omits them. Let me read the binding and the descriptor it fills.

--- L2188 ---
`solver_max_pairs` lives on `nuka_coupled_particles_desc_t`, not `nuka_world_desc_t` — so it isn't a pure binding gap. Before an ABI change, one thing to settle: the controller passes `contact_family=1` (PairDriven), whose manifolds are `ucontact_*`, and Python exports only the FUSED `contact_*` family.

--- L2200 ---
`contact_family=1` in the C ABI doesn't select a contact family at all — it bakes a heightfield collidable (`src/c_abi/world.cpp:334-338`). Let me see which solve feeds the `contact_force` readout.

--- L2206 ---
The readout reads `data.lambda` over the **unified PairDriven** buffer (`pipeline.cpp:687-688`), so `contact_force` does cover the general path — my wrong-family hypothesis is dead. But it indexes slots by `lambda`, whose row layout differs from `contact_points`. Let me check whether the probe's height filter was reading meaningful points at all.

--- L2221 ---
`contact_point` / `contact_link` / `contact_normal` are only consumed by `OpAssembleRowsFused` — the FUSED foot path. Nothing writes them on the PairDriven path, which explains `CONTACT_LINK` all zeros and `near_rim 0`: my telemetry was reading a buffer the active solver never fills. `contact_force` is real (it comes from `lambda`), but its slot indexing is FUSED-relative.

So the probe was blind, not the physics. Let me verify against the row-level data the PairDriven path actually populates.

--- L2227 ---
`LINK_CONTACT_WRENCH` is the per-link readout, and `LinkContactWrenchKernel` walks the actual solved rows (`row_cj_link`, `row_cj_link_b`) rather than FUSED slots. That's the correct instrument for "do the fingers feel the bowl". Let me probe with it.

--- L2240 ---
The bowl moved: `bowlz 0.9717` during "above" (from 0.8984), then settled back. Contacts exist and are doing work — but both readouts report exactly 0.000, so the telemetry itself is dead. `pipeline.cpp:691` gates the readout op behind `readout_demand`.

--- L2247 ---
`DemandReadout` enables the op lazily on first access. Let me check whether the C ABI / Python `buffer_view` path calls it.

--- L2254 ---
`FieldPtr` demands the readout, so the first `buffer_view` call turns the op on and backfills. My probe grabbed the view once before stepping under load — the backfill happened, then the pipeline rebuilt, but the plan re-capture means the op runs on subsequent steps. The zeros suggest the DLPack view isn't tracking the rebuilt buffer, or `stepped` uses the stale plan. Let me re-read the view after the pinch.

--- L2274 ---
`contact_force` is alive — `Fn sum 5.06`, max 0.61 N — so the readout works and contacts are solved. But `link_contact_wrench` is all zeros across every link, including the legs that clearly carry table load. That points at the wrench kernel's link attribution.

--- L2279 ---
Env 0's global index equals its local index, so the gate should match. Let me check who writes `row_cj_link`.

--- L2290 ---
`row_cj_link` is set only when `kind_a == kNkSideArtic`. If the Panda is cooked as an articulation, finger rows should tag. Let me check what `link_a` is relative to — it may be articulation-local while the kernel compares against a global link index.

--- L2300 ---
`ResolvePairSide` maps a shape's `body_id` through `body_to_link` / `body_to_articulation`. If the Panda's collision shapes were cooked as rigid bodies rather than articulation links, every side resolves to `kNkSideRigid`, `row_cj_link` stays `kInvalidLink`, and the wrench readout is identically zero — exactly what we see. Let me check the cook.

--- L2308 ---
Found it. The Panda exists **twice**: as articulation links 0-9 (link8/9 at z≈1.228) and as *rigid bodies* 0-9 plus 20-23 (rigid[8]/[9] at z≈1.184, tracking the fingers). `rigid_pose` has 246 rows, not 20.

So finger collision shapes are attached to rigid bodies, and `ResolvePairSide` returns `kNkSideRigid` — no articulation attribution, hence the zero wrench. More importantly, the finger contacts push *rigid* proxies that aren't coupled to the arm's articulation dynamics.

--- L2314 ---
The mechanism is general and correct: link-owned proxies resolve to `kNkSideArtic` through `body_to_link`. So the question is whether the finger proxies got `owner_link` set at cook time. Let me read the proxy seeding.

--- L2318 ---
The proxy path is correct. Note the ordering bug risk: `ResolvePairSide` redirects through `body_collidable_body` **before** reading `body_to_link`. For a link-owned proxy `body_collidable_body[row] == ~0u`, so no redirect — fine.

Let me check whether the finger proxy rows actually carry a valid `body_to_link` in this cooked scene.

--- L2328 ---
Line 545-546 is the constraint: only `Sphere`, `Box`, `Capsule` primitives claim a link. The Panda fingers are **mesh** collision geoms in the LIBERO MJCF. A mesh geom on an articulation link never enters this loop, so it gets no `owner_link` and no proxy — it stays a plain rigid body, decoupled from the arm.

--- L2340 ---
Confirmed. `cook_to_model.cpp:545-546` accepts only `Sphere | Box | Capsule` as link collision geometry. `ConvexHull` and `TriMesh` are silently skipped, so a mesh-collision articulation link gets `link_geom_kind == 0` and no proxy row — its collision shape stays an uncoupled rigid body.

That's why the fingers never transmit force to the bowl through the articulation: the arm's links have no collision representation at all.

--- L2344 ---
`model.hpp:181` documents the restriction explicitly: "only Sphere/Box/Capsule". Let me confirm the finger geoms in this scene are meshes, and check `sync_body_pose.cu` — links with `link_geom_kind == 0` are also excluded from pose sync, which strands their rigid proxies.

--- L2357 ---
The pads *are* boxes, so they pass the type filter — my mesh hypothesis is wrong. But `link_geom_kind` only records the **first** collision primitive; pads 2-5 become proxies. Let me test whether the pads reach the articulation at all by measuring what the fingers actually push.

--- L2361 ---
Decisive: closing on the rim and closing in mid-air produce **identical** joint states (0.00000). The rim offers zero resistance — the finger pads are not coupled to the bowl through the articulation contact path.

Let me confirm the pads produce collision shapes at all, via the same body/shape maps the cook writes.

--- L2367 ---
Let me check the broadphase filter — pads and bowl could be excluded from pair emission before any manifold exists.

--- L2378 ---
`contype`/`conaffinity` mask, and the shipped MJCF has `contype="0" conaffinity="0"` on the default geom class. Also there's a hard signal available: `kEnvStatusPairOverflow` in `ENV_STATUS`. Let me read both.

--- L2382 ---
The pad geoms inherit `class="collision"` which has no `contype`/`conaffinity` override, so they default to MuJoCo's 1/1 — collidable. `ENV_STATUS` bit 0 tells us directly whether pairs were dropped. Let me read it.

--- L2394 ---
`env_status` isn't exported either. Rather than keep chasing telemetry, let me test the hypothesis directly: if pair-budget starvation is the cause, giving the bowl fewer collision shapes must restore the grasp. `contact_family=0` also changes the path. Let me test the mechanism with a decisive experiment.

--- L2398 ---
Both families give identical results, and note the bowl **did** rise to 0.9046 (+0.0062 m) — so the pads do touch and push the bowl. But the fingers pass through to 0.00000 aperture regardless.

That combination means contact exists but the finger side transmits no reaction: the pads act as one-way pushers. This is the `link_geom_kind` proxy path — let me check whether the pad proxies got zero inverse mass.

--- L2406 ---
Proxy rows get `inv_mass = 0.0f` — massless, which is correct because the reaction is meant to route to the owner link via `body_to_link`, and `ResolvePairSide` handles that. So pads 2-5 are fine in principle.

But `link_geom_kind[owner_link]` is set from the **first** pad, meaning pad1 folds into the link's own body row. Let me verify the finger link's body row is actually dynamic and that its articulation reaction reaches the joint.

--- L2412 ---
The chain Jacobian is built from `row_cj_link`, so contact reaction reaches articulation DOFs only through it. Let me read `ComputeContactChainJacobianKernel` — specifically whether it walks the kinematic chain to the *gripper slide joints*.

--- L2416 ---
This is correct and general — it walks contact_link to root, including the finger slide joints. So the code path is sound; the failure must be that `row_cj_link` never names a finger link, i.e. the pads never resolve to `kNkSideArtic`.

Let me test that directly by checking whether the *table* pushes back on the fingers, which uses the exact same resolution.

--- L2420 ---
The articulation contact path works: the table blocked the gripper at z=0.9078 against a commanded 0.80. Finger links resolve to `kNkSideArtic` correctly, and the general solver transmits reaction into the arm.

So the failure is specific to the finger↔bowl pair. Given the table works and the bowl gets pushed, let me check the exclusion list — the bowl may be excluded from the finger proxies.

--- L2428 ---
The rim adds only +0.0001 m of standoff versus bare table — the gripper passes straight through the rim while the table stops it. Same links, same solver, same closed gripper: the table resists, the bowl does not.

Note the eef landed at x=-0.1877 rather than the commanded -0.075. Let me check whether the fingers ever actually reached the rim ring.

--- L2432 ---
Found it. Look at `row 13` — the bowl body — it moved to `x = +0.0583` from `-0.0750`. The gripper knocked the bowl **0.133 m sideways** while trying to straddle it.

And the fingers are at y=0.0277 and y=0.1077: a **0.080 m** separation straddling the rim ring correctly, but centered on y≈0.0677 while the rim ring center is y=0.015. The gripper is offset ~0.053 m off-axis, so one finger lands outside the bowl and the other plows through it.

The `+ RIM_R` waypoint I used is the bug in the probe: it targets the rim *edge*, not the bowl center.

--- L2436 ---
The pads sit at z = 0.9636/0.9612 while the rim is at 0.9320 — the fingers are **32 mm above** the rim even though the eef site is at 0.9170, below it. The pad collidables are *above* the eef site, not below it.

That's the real geometry error: my waypoints place the eef at the rim, which puts the actual pads well clear of it. Let me measure the true pad-to-eef offset.

--- L2440 ---
Clear now. The pad collidables (rows 20-27) sit within **±6 mm** of the eef site — they *are* essentially at the eef, straddling it in Y at ±0.027 m local. My earlier probe read link8/link9 origins (at z=0.96, 46 mm above the eef), not the pads.

So the pads span only 0.054 m in Y, not 0.080 m. Let me pinch with waypoints that place the *pads* on the rim wall.

--- L2448 ---
Straddle geometry was correct this time: pads at z 0.9219..0.9355 bracket the rim at 0.9320, pad y 0.0248..0.1092 brackets the wall at 0.0621. Yet closing knocked the bowl 0.11 m away and the fingers reached 0.00000.

Pad y spans 0.0248..0.1092 = 0.084 m, but the local offsets said ±0.027 (0.054 m). The pad rows are not the pair I assumed. Let me identify which rows are the *opposing* pads and check the exclusion list.

--- L2454 ---
Let me check what filter policy the importer generates — LIBERO MJCFs commonly carry broad `<contact><exclude>` sets, and the bowl↔pad pair may be excluded.

--- L2458 ---
There it is. The bowl rim boxes have `size="0.00071 0.00992 0.01187"` — a half-extent of **0.71 mm**, so the wall is 1.42 mm thick. And critically, `group="0"` with **no `contype`/`conaffinity`**, so they default to 1/1 — collidable.

The wall is 1.4 mm thick while the solver's contact margin and the pads' 0.5-1.7 mm closing step per tick are the same order. Let me check the margin the pipeline actually uses.

--- L2462 ---
`contact_margin` defaults to 0.0 (per `nuka.h:259`). With a 1.42 mm wall and zero margin, a pad crossing the wall in one tick can tunnel clean through before a manifold ever forms. That fits every observation: the bowl gets shoved (glancing hits register) but a closing pinch passes through.

Let me test this by raising the margin. The C ABI honors it; I need the Python knob.

--- L2470 ---
Let me look at the C++ `World::create_from_scene` wrapper to see how it fills the descriptor.

--- L2474 ---
`nuka_world_desc_t` fills fields directly. Let me confirm the descriptor carries the solver knobs.

--- L2480 ---
The knobs sit on `nuka_world_desc_t` (nuka.h:261-264) and `world.cpp:485-486` applies them. `create_from_scene` in the binding simply never sets them. Adding them is a general solver knob, exactly the right shape of fix.

--- L2488 ---
Now I'll add the four solver knobs to `create_from_scene`, mirroring the coupled path exactly.

--- L2499 ---
Now the docstring, appending to the existing text.

--- L2513 ---
Let me verify the descriptor field names match before building.

--- L2517 ---
These fields are on `nuka_coupled_particles_desc_t`, not `nuka_world_desc_t` — my edit sets fields that don't exist on the world desc. Let me check what `nuka_world_desc_t` actually has.

--- L2521 ---
Confirmed: `nuka_world_desc_t` has no solver fields — they live only on the coupled particles desc. So this requires extending the world desc and the C ABI entry point too. Let me revert the invalid desc assignments and check how the C ABI create function is wired.

--- L2525 ---
Now extend `nuka_world_desc_t` with the same five knobs. Let me see its tail.

--- L2533 ---
Now wire them in the C ABI implementation. Let me find where `create_from_scene` builds its SolverConfig.

--- L2541 ---
There's already a shared helper taking the solver overrides. Let me read it and the `create_from_scene` call site.

--- L2545 ---
The helper already accepts all four knobs — the scene-only path just passes zeros. Let me find that call and forward the desc values, plus the baumgarte model field.

--- L2553 ---
The helper's default args for the solver params need checking — the coupled path passes them positionally, so let me confirm the declaration allows the scene-only call I just wrote.

--- L2564 ---
Defaults are already in place, so the signature works. Now build.

--- L2583 ---
The Python extension isn't in this CMake tree — it builds separately. Let me check how it's normally built and where the installed module lives.

--- L2590 ---
scikit-build-core with a `build/{wheel_tag}` cache. Let me rebuild and reinstall the wheel.

--- L2598 ---
CMake isn't on PATH for the subprocess. Let me add it and retry.

--- L2605 ---
Built and installed. Let me verify the knobs are exposed.

--- L2609 ---
The wheel built from a cached build dir and didn't pick up my source edits. Let me check whether the edits are actually in the file and force a clean configure.

--- L2616 ---
API Error: Request rejected (429) · Too many pending requests, please retry later

--- L2622 ---
The shell cwd drifted into `python/` — that's why the greps missed. Let me verify my edits landed.

--- L2630 ---
Edits are in place at `python/src/nuka_ext.cpp:1894` and `nuka.h:191-195`. The wheel built stale — scikit-build-core reused `build/{wheel_tag}` without reconfiguring. Let me clear that cache and rebuild.

--- L2634 ---
API Error: Request rejected (429) · Too many pending requests, please retry later